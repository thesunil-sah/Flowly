// Flowly tracker — placeholder (real pageview logic lands in Phase 1/2).
// Must never throw and never break the host page: wrap everything, fail silently.
(function () {
  try {
    // no-op for now
  } catch (_) {
    /* fail silently */
  }
})();
